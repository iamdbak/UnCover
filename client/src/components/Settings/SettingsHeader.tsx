import React, { Fragment } from "react";
import "./Settings.css"; 

//component will have tags to the Account, Notifications, Contact, Privacy and Safety sections of the Settings Page

const SettingsHeader: React.FC = () => {
    return (
        <div className="settingsheader">
        </div>
    );
}

export default SettingsHeader;
